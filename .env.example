# Example environment variables (copy to `.env` and fill values)
# Do NOT commit secrets to the repository. This file is safe to commit.

# Vite public variables must start with VITE_
VITE_API_URL=
VITE_PUBLIC_KEY=
# Environment Variables

# Shared password for authentication (required)
ARCHIVE_PASSWORD=LucaSuca666


# For production database (optional - currently using in-memory store)
# DATABASE_URL=your-database-connection-string
